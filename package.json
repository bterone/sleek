{
  "name": "sleek-css-test",
  "version": "1.0.3",
  "description": "Just a test library",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "sass ./src/scss/index.scss ./dist/index.css && node ./bin/build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "yarn build"
  },
  "files": [
    "dist/*",
    "src/*"
  ],
  "author": "bterone",
  "main": "./dist/index.js",
  "style": "./dist/index.css",
  "sass": "./src/scss/index.scss",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.14.10",
    "rimraf": "^3.0.2",
    "sass": "^1.46.0"
  }
}
